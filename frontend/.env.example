# Point at the deployed API before demo day — never leave this on localhost.
VITE_API_BASE_URL=http://localhost:5000

# Must match GOOGLE_CLIENT_ID on the backend. Leave blank to hide the Google
# button and use demo accounts only.
VITE_GOOGLE_CLIENT_ID=
